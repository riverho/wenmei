# 2026-07-08 Session Log

## Current product/design state

Phase F is implemented in the real app and the `app_design/` playground remains
the accepted visual source. The playground is not junk; it is the ongoing
visual proving ground. When visual behavior differs, use the playground as the
directional reference and port deliberately into `src/`.

## app_design/ — accepted playground direction

### New files

- `src/lib/sidecar-types.ts` — data model (SidecarItem, SidecarArtifact, SidecarDiffLine, truncateBody, isLongContent, buildSummary, extractArtifacts, extractFileRefs)
- `src/components/SidecarFeed.tsx` — unified feed: chat is base layer, everything else is overlay. Chat-only mode via inputActive state + filter=chat. Notification dots replace overlay chips in chat-only mode. Persistent chat input bar at bottom.
- `src/components/SidecarDetail.tsx` — 90% overlay for full message inspection
- `src/components/SettingsPanel.tsx` — 8-section settings panel (General, Terminal, Windows, Keyboard Shortcuts, Agent & Narration, Integrations, License, About)

### Key UX behavior (SidecarFeed)

- `inputActive` state: true when user types in chat bar
- When `inputActive || filter === "chat"`: only chat items shown, overlay chips replaced by notification dots
- When in other filter modes: all overlay items shown (review, narration, alerts)
- `Chat` chip always shown + focuses input
- `All` chip always shown (restores full overlay view)
- Filter chips (Narrate/Alerts/Review) shown as dots in chat-only mode, full chips otherwise

### Modified files

- `src/App.tsx` — RightPanel now renders `<SidecarFeed />` directly
- `src/components/Lightbox.tsx` — replaced inline SettingsPanel (661–1463 lines) with `import SettingsPanel from "./SettingsPanel"`
- `src/components/TerminalPanel.tsx` — full F4 wiring: TerminalTabBar, add/close tabs, per-tab narration toggle, memory indicator
- `src/index.css` — added sidecar-detail-in/out keyframes, kbd styles
- `src/main.tsx` — back to clean StrictMode wrap

### TypeScript: all clean (tsc -b passes)

### Build: clean (vite build succeeds)

## src/ — real app Phase F port status

### Implemented from playground / Phase F

- F11: `src/lib/sidecar-types.ts` and `src/lib/sidecar-feed.ts` are in the real app.
- F11: `src/components/PiPanel.tsx` assembles overlay items from narration, notifications, review changes, and journal hydration.
- F11: unread state persists through `sidecarLastSeen` in `src/store/appStore.ts`.
- F12: `src/components/SidecarDetail.tsx` is ported and wired from overlay cards.
- F13: `src/components/SettingsPanel.tsx` is the full playground settings panel adapted to real bridge/store APIs.
- F13: machine settings persist through Rust `AppState`, `src/lib/tauri-bridge.ts`, and Zustand autosave.
- F14: `src/components/Notifications.tsx` is mounted in `Header`, uses sidecar alert unread count, and deep-links PiPanel to the Alerts filter.

### Important integration note

The real app intentionally does not render `app_design/src/components/SidecarFeed.tsx`
wholesale. The accepted feed mechanics were adapted into `PiPanel.tsx` so the
existing real Pi chat/composer pipeline remains intact while overlay cards,
filters, unread state, and detail expansion match the playground direction.

## Phase F close criteria

1. Treat `app_design/` as accepted ongoing playground, not cleanup trash.
2. Keep `design-contract.yaml` updated whenever the visual direction changes.
3. Close Phase F only after real app checks pass and a desktop build/check confirms the wiring.
4. Next cycle should start from the consolidated contract and memory, not from stale F4/F5/F6 notes.

## Phase F close checks (20:38 Asia/Hong Kong)

- `design-contract.yaml` parses as YAML.
- Root app: `npm run format`, `npm run lint`, `npm run check`, `npm run test` passed.
- Rust: `cargo check` passed with existing unused notification constant warnings.
- Playground: `app_design npm run check` and `app_design npm run build` passed.
- Desktop: `npm run desktop:build -- --bundles app` produced `src-tauri/target/release/bundle/macos/Wenmei.app`.
- Live smoke: installed `/Applications/Wenmei.app` responded to `wenmeictl status` when pointed at the app-support control file. Local repo `.wenmei/wenmei-control.json` is stale and can shadow the live app unless `WENMEI_CONTROL_FILE` is set.

## v1.0 RC Go/No-Go checklist (Phase G, updated 10 Jul 2026)

Go when every box is checked; L4 (tag/release/announce) stays blocked until
River's explicit sign-off regardless.

- [ ] RC2 — Finder/open path bug fixed: vault-relative paths format correctly
      on OS file-open events.
- [ ] RC3 — `wenmeictl` control discovery ignores stale repo-local
      `.wenmei/wenmei-control.json` (app-support control file wins).
- [ ] RC4 — background poller no longer triggers avoidable macOS permission
      dialogs (scoped WalkDir).
- [ ] RC5 — full RC validation sweep green: root format/lint/check/test,
      app_design check/build, cargo check, desktop bundle build, installed-app
      wenmeictl smoke.
- [ ] Signing/notarization decision recorded (ship unsigned with docs, or
      configure certs in release.yml).
- [ ] Human end-to-end pass on the installed app: narrate-on-default terminal,
      overlay feed + detail pane, bell deep-link, settings round-trip restart.
- [ ] Draft GitHub release reviewed by River → publish is a human click (L4).

No-go triggers: any check regression, data-loss path in review/reject flow,
or unresolved permission-dialog spam on first launch.

## Phase H boot-verify (11 Jul 2026)

Fresh keyless release bundle built clean (after H8b fix); `/Applications`-
style launch of the built `.app` produced no crash.log (panic hook + control
server + heartbeat thread + updater plugin all init clean), wrote its
control file, and answered `wenmeictl status` plus the new `wenmeictl
orchestrator status` (H11 surface live: runs/changeset/pending_prompt). H6
run-card listing exercised via that call. Compile + runtime both green.

### Go/no-go delta

- [x] RC5-equivalent for Phase H: keyless desktop build + boot smoke green.
- [ ] Generate updater keypair (`npm run tauri signer generate`) → set
      TAURI_SIGNING_PRIVATE_KEY secret + real pubkey in tauri.conf.json
      (unblocks H8 runtime update checks).
- [ ] Human validation run to lift the H10/H11 runtime gate: drive a real
      agent to a permission prompt, confirm the input.needs_response alert +
      Allow/Deny injection with verify-then-act; confirm a checker `risk`
      verdict raises safety.checker_flag.
- [ ] L4 launch actions remain blocked on River's explicit sign-off.
